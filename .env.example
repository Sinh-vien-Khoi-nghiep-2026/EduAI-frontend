# ArborCursus API origin; no trailing slash. Only BUN_PUBLIC_* variables are embedded in browser bundles.
BUN_PUBLIC_API_BASE_URL=http://localhost:8000

# bun dev serves this app at http://localhost:8081, matching ArborCursus's default CORS_ORIGINS.
