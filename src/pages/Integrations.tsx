import { useQuery } from "@tanstack/react-query";
import { CheckCircle2, CircleAlert, Link } from "lucide-react";
import { arbor } from "@/api/arbor";
import { useSession } from "@/auth/session";
import { ErrorBlock, LoadingBlock, PageHeader, Panel, Status } from "@/components/app-ui";
export default function Integrations(){const {token}=useSession();const rows=useQuery({queryKey:["integrations"],queryFn:()=>arbor.integrations(token!)});if(rows.isLoading)return <LoadingBlock label="Loading integrations…"/>;if(rows.isError)return <ErrorBlock message="Integration status could not be loaded." retry={()=>void rows.refetch()}/>;return <div><PageHeader eyebrow="Connection status" title="Integrations" description="This page reflects integrations the backend currently exposes. It does not offer unsupported connection or synchronization controls."/><div className="integration-grid">{rows.data?.map(item=><Panel key={item.provider} className="integration-card"><div className="integration-head"><span className="provider-mark">{item.provider==="linkedin"?<Link size={20}/>:<CheckCircle2 size={20}/>}</span><div><h2>{item.provider}</h2><Status tone={item.status==="connected"||item.status==="available"?"success":"warning"}>{item.status}</Status></div></div><div className="capability-list">{Object.entries(item.capabilities).map(([name,available])=><div key={name}>{available?<CheckCircle2 size={15}/>:<CircleAlert size={15}/>}<span>{name.replaceAll("_"," ")}</span><strong>{available?"Available":"Unavailable"}</strong></div>)}</div>{item.unsupported&&<p className="integration-note">Unavailable: {item.unsupported.join(", ")}.</p>}{item.error&&<p className="form-error">{item.error}</p>}{item.last_sync_at&&<p className="muted">Last sync: {new Date(item.last_sync_at).toLocaleString()}</p>}</Panel>)}</div></div>}
