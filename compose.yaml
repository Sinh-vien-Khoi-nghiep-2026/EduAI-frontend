services:
  web:
    build: .
    ports:
      - "8081:8081"
    environment:
      NODE_ENV: production
      PORT: 8081
      BUN_PUBLIC_API_BASE_URL: ${BUN_PUBLIC_API_BASE_URL:-http://localhost:8000}